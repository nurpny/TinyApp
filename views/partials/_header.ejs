<!DOCTYPE html>
<html>
  <head></head>
    <h1>TinyApp</h1>
    <% if (user) { %>
      <h2> Welcome <%= user.email %>!</h2>
        <form action="/logout" method="POST">
          <input type="submit" value="Log Out">
        </form>
        <% } else { %>
      <p>Please <a href="/register">register</a> or <a href="/login">log in</a> to use TinyApp</p>
    <% } %>
</html>